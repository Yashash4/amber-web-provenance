# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.venv/
venv/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Signing keys MUST NEVER be committed.
# The demo signer key below is the one exception: it is a clearly-labelled
# DEMO-ONLY key committed so anyone cloning the repo can reproduce the signed
# golden packet. Real operator keys live outside the repo.
*.key
!amber/keys/demo-signer.key
secrets/
*.private
